%p#notice= notice
%h1 Tournaments
%table.table
  %thead
    %tr
      %th{scope: "col"} Title
      %th{scope: "col"} Nb. contestants
      %th{:colspan => "3"}
  %tbody
    - @tournaments.each do |tournament|
      %tr
        %td{scope: "row"}= tournament.title
        %td{scope: "row"}= tournament.contestants.count
        %td= link_to 'Show', tournament, class: "btn btn-link"
        %td= link_to 'Edit', edit_tournament_path(tournament), class: "btn btn-link"
        %td= link_to 'Destroy', tournament, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link"
%br/
= link_to 'New Tournament', new_tournament_path
